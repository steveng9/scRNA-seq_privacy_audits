[CLI]
# Keep all of Manim's scratch build output (videos/images/texts/Tex/logs)
# nested under presentation/manim/media/ instead of spilling out to a
# repo-root media/ folder. This only takes effect when manim is run with
# this directory as the working directory (render.sh does that).
media_dir = media
